@model KGID_Models.KGID_GroupInsurance.GIS_Ledger

@{
    ViewBag.Title = "GIS Ledger";

}
@using (Html.BeginForm("EmployeeGISLedger", "GroupInsuranceClaim", FormMethod.Get))
{
   

    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-header">
                <h6 class="m-0 font-weight-bold text-primary">

                    <label class="Enlan control-label">Employee Ledger</label>
                </h6>
            </div>
            <div class="card-body">
                @*<div class="row">
                    <div class="form-group col-6">
                        <label for="lblKGIDNo" class=" control-label">KGID No :</label>
                        <div class="row">
                            <div class="form-group col-4">
                                <input type="text" name="empsearch" />
                            </div>
                            <div class="form-group col-2">
                                <input type="submit" name="submit" value="Get Employee Details" class="btn btn-sm btn-primary" />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <table class="table table-bordered table-condensed">
                        <tr>
                            <th>
                                
                                @Html.LabelFor(model => model.employee_id, "Employee Id", new { @class = "Enlan control-label" }) Employee ID
                                @Html.TextBoxFor(model => model.employee_id, new { @class = "form-control", @style = "text-transform:uppercase", @id = "txtemployeeid", @maxlength = "25", @readonly = "readonly" })
                            </th>
                            <th>
                                @Html.LabelFor(model => model.employee_name, "Employee Name", new { @class = "Enlan control-label" })
                                @Html.TextBoxFor(model => model.employee_name, new { @class = "form-control", @style = "text-transform:uppercase", @id = "txtemplyeename", @maxlength = "25", @readonly = "readonly" })
                            </th>
                        </tr>
                    </table>
                </div>*@
                <div class="row">
                    <div class="col-12">
                        <table class="table table-striped table-bordered" id="tblApplicationList" cellspacing="0">
                            <thead style="background: #308385;color: #fff;">
                                <tr>

                                    <th>Sr No</th>
                                    <th>Mon Year</th>
                                    <th>Opening</th>
                                    <th>Contri</th>
                                    <th>Int Rate</th>
                                    <th>Int </th>                                    
                                    <th>Closing</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model.listgisledgerdet)
                                {
                                <tr>
                                    <td>@item.SR</td>
                                    <td>@item.MNYEAR</td>
                                    <td>@item.CURR_OPN</td>
                                    <td>@item.CONT_GIS</td>
                                    <td>@item.INT_RATE</td>
                                    <td>@item.INT_AMT</td>
                                    <td>@item.CURR_CLS</td>
                                </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

}